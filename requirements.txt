# Helper dependencies for the local orchestration scripts in this repository.
# The actual training stack is installed from the official microWakeWord repo
# in setup_environment.sh.

datasets>=2.21.0,<3.0.0
mmap-ninja>=0.8.0,<1.0.0
pyyaml>=6.0,<7.0
requests>=2.32.0,<3.0.0
soundfile>=0.12.1,<1.0.0

# Wake Word Trainer Requirements
# Install with: pip install -r requirements.txt

# TTS sample generation
edge-tts>=6.0.0

# Data handling
# Python 3.12 support is much safer with newer Hugging Face Datasets releases.
datasets>=2.19.2,<3.0.0
huggingface_hub>=0.20.0
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.10.0

# Configuration
pyyaml>=6.0
requests>=2.28.0
tqdm>=4.65.0

# NumPy (must be <2.0 for TensorFlow compatibility)
numpy>=1.24.0,<2.0

# PyArrow 14.x has Linux wheels for Python 3.12, avoiding a source build on Ubuntu 24.04.
pyarrow>=14.0.1,<15.0.0

# TensorFlow (2.16.x - requires patch for microWakeWord compatibility)
tensorflow>=2.16.0,<2.17.0

# mmap for feature storage
mmap-ninja>=0.8.0

# Audio processing
webrtcvad>=2.0.10
audioread>=3.0.0

# Training backend used by oww_wrapper.py
nanowakeword[train]>=2.0.2,<2.1
